{
  "name": "siphon-cloudflare-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers + Containers demo deployment for siphon-api",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.128.0"
  }
}
