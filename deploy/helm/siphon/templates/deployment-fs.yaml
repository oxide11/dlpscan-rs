{{- if .Values.fs.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "siphon.componentName" (dict "root" . "component" "fs") }}
  labels:
    {{- include "siphon.labels" (dict "root" . "component" "fs") | nindent 4 }}
spec:
  replicas: {{ .Values.fs.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      {{- include "siphon.selectorLabels" (dict "root" . "component" "fs") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "siphon.labels" (dict "root" . "component" "fs") | nindent 8 }}
      annotations:
        checksum/overrides: {{ include (print $.Template.BasePath "/configmap-overrides.yaml") . | sha256sum }}
        {{- include "siphon.podAnnotations" (dict "root" .) | nindent 8 }}
    spec:
      terminationGracePeriodSeconds: 60  # large uploads may be in-flight
      # siphon-fs doesn't talk to the k8s API; the default SA
      # token would just be unused attack surface inside the
      # container. Block the auto-mount.
      automountServiceAccountToken: false
      {{- with include "siphon.imagePullSecrets" (dict "root" .) }}
      imagePullSecrets:
        {{- . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: siphon-fs
          image: {{ include "siphon.image" (dict "root" . "image" .Values.fs.image) }}
          imagePullPolicy: {{ default .Values.global.imagePullPolicy .Values.fs.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.fs.service.port }}
              protocol: TCP
          env:
            - name: SIPHON_FS_BIND
              value: "0.0.0.0:{{ .Values.fs.service.port }}"
            - name: SIPHON_OVERRIDES_PATH
              value: /etc/siphon/overrides.json
            - name: RUST_LOG
              value: info
            {{- if .Values.postgres.enabled }}
            # Bundled Postgres — same URL shape as siphon-api so the same
            # migration schema is shared.  Password is in a separate env var
            # that the driver appends; the URL carries no credentials in plain.
            - name: SIPHON_DATABASE_URL
              value: {{ printf "postgres://%s@%s:%v/%s"
                .Values.postgres.username
                (include "siphon.componentName" (dict "root" . "component" "postgres"))
                .Values.postgres.service.port
                .Values.postgres.database | quote }}
            - name: SIPHON_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ required "postgres.secretName is required when postgres.enabled is true" .Values.postgres.secretName }}
                  key: password
            - name: SIPHON_DATABASE_TLS
              value: {{ .Values.postgres.sslMode | quote }}
            {{- with .Values.postgres.caFile }}
            - name: SIPHON_DATABASE_CA_FILE
              value: {{ . | quote }}
            {{- end }}
            {{- else if .Values.externalPostgres.enabled }}
            - name: SIPHON_DATABASE_URL
              {{- if and .Values.externalPostgres.urlSecretRef.name .Values.externalPostgres.urlSecretRef.key }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.externalPostgres.urlSecretRef.name }}
                  key: {{ .Values.externalPostgres.urlSecretRef.key }}
              {{- else }}
              value: {{ required "externalPostgres.url or externalPostgres.urlSecretRef must be set when externalPostgres.enabled is true" .Values.externalPostgres.url | quote }}
              {{- end }}
            - name: SIPHON_DATABASE_TLS
              value: {{ .Values.externalPostgres.sslMode | quote }}
            {{- with .Values.externalPostgres.caFile }}
            - name: SIPHON_DATABASE_CA_FILE
              value: {{ . | quote }}
            {{- end }}
            {{- end }}
            {{- if .Values.tls.internal.enabled }}
            # siphon-fs served plaintext and relied on the mesh. Uploads are
            # the data the scanner exists to find; they now cross the pod
            # network only to a peer holding nginx's certificate.
            - name: SIPHON_FS_TLS_CERT
              value: /etc/siphon/tls/tls.crt
            - name: SIPHON_FS_TLS_KEY
              value: /etc/siphon/tls/tls.key
            - name: SIPHON_FS_TLS_CLIENT_CA
              value: /etc/siphon/tls/ca.crt
            {{- end }}
            {{- with .Values.fs.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: overrides
              mountPath: /etc/siphon
              readOnly: true
            - name: scratch
              mountPath: /tmp
            {{- if .Values.tls.internal.enabled }}
            - name: tls
              mountPath: /etc/siphon/tls
              readOnly: true
            {{- end }}
            {{- with .Values.fs.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          {{- if .Values.tls.internal.enabled }}
          # kubelet cannot present a client certificate, so an httpGet probe
          # would be refused in the handshake. tcpSocket confirms the
          # listener is up, which is what siphon-api's probes already do.
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: {{ .Values.fs.livenessProbe.initialDelaySeconds | default 5 }}
            periodSeconds: {{ .Values.fs.livenessProbe.periodSeconds | default 15 }}
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: {{ .Values.fs.readinessProbe.initialDelaySeconds | default 2 }}
            periodSeconds: {{ .Values.fs.readinessProbe.periodSeconds | default 3 }}
            failureThreshold: {{ .Values.fs.readinessProbe.failureThreshold | default 3 }}
          {{- else }}
          livenessProbe:
            {{- toYaml .Values.fs.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.fs.readinessProbe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.fs.resources | nindent 12 }}
      volumes:
        - name: overrides
          configMap:
            name: {{ include "siphon.fullname" . }}-overrides
            items:
              - key: overrides.json
                path: overrides.json
        - name: scratch
          {{- if eq .Values.fs.scratchVolume.kind "pvc" }}
          persistentVolumeClaim:
            claimName: {{ include "siphon.componentName" (dict "root" . "component" "fs") }}-scratch
          {{- else }}
          emptyDir:
            sizeLimit: {{ .Values.fs.scratchVolume.sizeLimit }}
          {{- end }}
        {{- if .Values.tls.internal.enabled }}
        - name: tls
          secret:
            secretName: {{ include "siphon.internalTlsSecret" (dict "root" . "id" "fs") }}
            defaultMode: 0440
        {{- end }}
        {{- with .Values.fs.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
