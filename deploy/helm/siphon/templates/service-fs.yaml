{{- if .Values.fs.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "siphon.componentName" (dict "root" . "component" "fs") }}
  labels:
    {{- include "siphon.labels" (dict "root" . "component" "fs") | nindent 4 }}
spec:
  type: {{ .Values.fs.service.type }}
  ports:
    - port: {{ .Values.fs.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "siphon.selectorLabels" (dict "root" . "component" "fs") | nindent 4 }}
{{- end }}
