{{- if .Values.authelia.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "siphon.componentName" (dict "root" . "component" "authelia") }}
  labels:
    {{- include "siphon.labels" (dict "root" . "component" "authelia") | nindent 4 }}
spec:
  type: {{ .Values.authelia.service.type }}
  ports:
    - port: {{ .Values.authelia.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "siphon.selectorLabels" (dict "root" . "component" "authelia") | nindent 4 }}
{{- end }}
