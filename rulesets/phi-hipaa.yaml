name: PHI HIPAA
description: >
  HIPAA-aligned ruleset for detecting protected health information.
  Covers the 18 HIPAA identifiers including medical records, biometric
  data, insurance information, and device identifiers.

version: "1"

baselines:
  - phi

action: redact
mode: denylist
min_confidence: 0.5
require_context: false

overrides:
  - category: Device Identifiers
    min_confidence: 0.7
    require_context: true
  - category: Contact Information
    min_confidence: 0.6
